name: server
description: A server app using the shelf package and Docker.
version: 1.0.0

environment:
  sdk: '>=2.15.0 <3.0.0'

dependencies: 
  grpc: ^3.0.2
  protobuf: ^2.0.1

dev_dependencies:
  lints: ^1.0.0
