import 'package:flutter/material.dart';

class LoginAccountField extends StatelessWidget {
  const LoginAccountField({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const TextField();
  }
}
